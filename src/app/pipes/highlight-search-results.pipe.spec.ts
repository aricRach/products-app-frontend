import { HighlightSearchResultsPipe } from './highlight-search-results.pipe';

describe('HighlightSearchResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightSearchResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
